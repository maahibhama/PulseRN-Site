<div class="code-window" aria-label="PulseRN SDK installation example">
  <div class="window-bar" aria-hidden="true">
    <i></i><i></i><i></i><span>App.tsx</span>
  </div>
  <pre><code><span class="comment">// Install the SDK</span>
<span class="prompt">$</span> pnpm add @pulse-rn/sdk

<span class="keyword">import</span> &#123; PulseRN &#125; <span class="keyword">from</span> <span class="string">'@pulse-rn/sdk'</span>;

PulseRN.<span class="method">connect</span>(&#123;
  appName: <span class="string">'My React Native App'</span>,
  enableConsole: <span class="boolean">true</span>,
&#125;);</code></pre>
</div>
